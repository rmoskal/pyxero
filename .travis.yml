language: python
python:
  - "3.3"
  - "3.4"
  - "2.7"
  - "pypy"
# command to install dependencies, e.g. pip install -r _requirements_.txt --use-mirrors
install: python setup.py install
# command to run tests, e.g. python setup.py test
script: python setup.py test
